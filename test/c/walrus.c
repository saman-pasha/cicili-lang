typedef struct point { int x; double y; } point_t;
g := 7;
int f(point_t p, char *s, const int k) {
    n := 42;
    d := n + 1.5;
    q := &p;
    y := q->y;
    t := s;
    c := k;
    for (i := 0; i < n; i++) { n += i; }
    return n + g;
}
